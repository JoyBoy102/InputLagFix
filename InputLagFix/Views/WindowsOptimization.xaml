<UserControl x:Class="INPUTLAGFIX.Views.WindowsOptimization"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:INPUTLAGFIX.Views"
             xmlns:vm="clr-namespace:INPUTLAGFIX.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1600">
    <UserControl.DataContext>
        <vm:WindowsOptimizationViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <ListBox Grid.Column="0" 
                 Name="CategoriesList" 
                 ItemsSource="{Binding SettingsCategories}" 
                 SelectedItem="{Binding SelectedCategory}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding CategoryName}" 
                       TextWrapping="Wrap" 
                       Margin="2"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ListBox x:Name="CategoryListBox" Grid.Column="1" ItemsSource="{Binding SelectedCategory.Settings}" MaxHeight="700" ScrollViewer.VerticalScrollBarVisibility="Visible">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox Content="{Binding ruName}" IsChecked="{Binding CheckedState}"></CheckBox>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Grid.Row="1" Content="Применить" Width="200" Height="30" Command="{Binding ApplySettingsCommand}" Grid.ColumnSpan="2"></Button>
        <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="LightGray" Grid.ColumnSpan="2">
            <ListBox ItemsSource="{Binding LogMessages}" 
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Visible">          
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontFamily="Consolas" Margin="2"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</UserControl>
