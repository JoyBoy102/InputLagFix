<UserControl x:Class="INPUTLAGFIX.Views.LoadingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:INPUTLAGFIX.Views"
             xmlns:gif="http://wpfanimatedgif.codeplex.com"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="650"
             MaxHeight="800"
             MaxWidth="650">
    <Grid>
        <Border CornerRadius="20"
                Padding="5"
                Background="#151F28"
                Margin="20"
                Grid.Column="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Margin="20" Foreground="White" Text="Подождите, идет оптимизация..." VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="24"></TextBlock>
                
            </Grid>
        </Border>
    </Grid>
</UserControl>
